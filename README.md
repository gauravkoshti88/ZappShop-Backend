# ZappShop-Backend
The backend is developed using Node.js, Express.js, and MongoDB, with secure APIs for product, cart, and order management. JWT Authentication ensures protected user sessions, and a payment gateway integration enables secure transactions. The backend is deployed on Render for reliable server hosting.
